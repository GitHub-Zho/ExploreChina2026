---
import Layout from '../../layouts/Layout.astro';
import {
  currentTrip,
  faq,
  included,
  notIncluded,
  cityGuides,
  itinerary,
  recommendedVideos,
} from '../../data/site';

// Pre-compute slides for each day in the frontmatter (TypeScript safe)
type LandmarkItem = { name: string; nameZh: string; image: string; background: string; backgroundZh: string };
type CitySlide = { type: 'city'; guide: typeof cityGuides[0] };
type LandmarkSlide = { type: 'landmark'; lm: LandmarkItem };
type DaySlide = CitySlide | LandmarkSlide;

const itineraryWithSlides = itinerary.map((d, idx) => {
  const isFirstCityDay = idx === 0 || itinerary[idx - 1].city !== d.city;
  const cityGuide = cityGuides.find(g => g.city === d.city);
  const landmarks: LandmarkItem[] = (d as any).landmarks ?? [];
  const slides: DaySlide[] = [
    ...(isFirstCityDay && cityGuide ? [{ type: 'city' as const, guide: cityGuide }] : []),
    ...landmarks.map(lm => ({ type: 'landmark' as const, lm })),
  ];
  return { ...d, isFirstCityDay, slides };
});

const tripStructuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'TouristTrip',
    name: currentTrip.title,
    description:
      'A 10-day student-led China summer trip from Canada covering Shanghai, Suzhou, Hangzhou, and Beijing.',
    itinerary: itinerary.map((day) => `${day.day}: ${day.city} - ${day.title}`),
    touristType: 'Students and young travelers from Canada',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://www.explorechina.ca/' },
      { '@type': 'ListItem', position: 2, name: 'Trip', item: 'https://www.explorechina.ca/trips/explore-china-2026-summer' },
    ],
  },
];
---
<Layout
  title={currentTrip.title}
  description="2026 summer student trip to China from Canada. 10-day route: Shanghai, Suzhou, Hangzhou, Beijing. Includes accommodation, transport, cultural activities. ~CAD $3,000."
  lang="en"
  alternateHref="/zh/trips/explore-china-2026-summer"
  alternateLabel="中文"
  canonical="/trips/explore-china-2026-summer"
  structuredData={tripStructuredData}
  darkNav={true}
>

  <div class="trip-hero">
    <img
      src="/images/highlights/forbidden-city.jpg"
      alt="Forbidden City, Beijing"
      class="trip-hero__img"
    />
    <div class="trip-hero__overlay">
      <h1 class="trip-hero__title">{currentTrip.title}</h1>
      <p class="trip-hero__sub">{currentTrip.summary}</p>
    </div>
  </div>

  <section id="overview" class="trip-strip">
    <div class="trip-strip__inner">
      <div class="trip-strip__facts">
        <div class="trip-strip__fact"><span class="trip-strip__label">Route</span><span class="trip-strip__val">{currentTrip.subtitle}</span></div>
        <div class="trip-strip__fact"><span class="trip-strip__label">Departure</span><span class="trip-strip__val">{currentTrip.departureWindow}</span></div>
        <div class="trip-strip__fact"><span class="trip-strip__label">Price</span><span class="trip-strip__val trip-strip__val--price">{currentTrip.priceCad}</span></div>
        <div class="trip-strip__fact"><span class="trip-strip__label">Group</span><span class="trip-strip__val">{currentTrip.groupSize}</span></div>
        <div class="trip-strip__fact"><span class="trip-strip__label">Deadline</span><span class="trip-strip__val">{currentTrip.applicationDeadline}</span></div>
      </div>
      <div class="trip-strip__actions">
        <a class="trip-cta" href="#apply">Apply for This Trip</a>
      </div>
    </div>
  </section>

  <!-- Highlights Strip -->
  <section class="highlights-strip" aria-label="Route highlights">

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1545569341-9eb8b30979d9?auto=format&fit=crop&w=600&q=80" alt="The Bund Shanghai" loading="lazy" />
      <p class="highlights-strip__caption">A waterfront built to impress empires — still working</p>
    </div>

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1563245372-f21724e3856d?auto=format&fit=crop&w=600&q=80" alt="Xiaolongbao Shanghai soup dumplings" loading="lazy" />
      <p class="highlights-strip__caption">Soup inside a dumpling. The technique took 600 years to perfect.</p>
    </div>

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1535082623926-b39352a03fb7?auto=format&fit=crop&w=600&q=80" alt="Suzhou classical garden" loading="lazy" />
      <p class="highlights-strip__caption">Gardens designed to make you forget which century you're in</p>
    </div>

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1500039436846-25ae2f11882e?auto=format&fit=crop&w=600&q=80" alt="West Lake Hangzhou" loading="lazy" />
      <p class="highlights-strip__caption">A lake so famous it was written into the national consciousness</p>
    </div>

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1508804185872-d7badad00f7d?auto=format&fit=crop&w=600&q=80" alt="Peking duck Beijing" loading="lazy" />
      <p class="highlights-strip__caption">Duck roasted at 200°C — unchanged since the Ming Dynasty</p>
    </div>

    <div class="highlights-strip__card">
      <img src="https://images.unsplash.com/photo-1547981609-4b6bfe67ca0b?auto=format&fit=crop&w=600&q=80" alt="Forbidden City Beijing" loading="lazy" />
      <p class="highlights-strip__caption">9,999 rooms. Only one person was allowed to count them.</p>
    </div>

  </section>

  <section class="route-intro">
  <div class="route-intro__inner">
    <div class="route-intro__rule"></div>

    <p class="route-intro__headline">
      This is our signature route — and it might look like every other China itinerary at first glance.
    </p>

    <div class="route-intro__body">
      <p>
        Shanghai, Suzhou, Hangzhou, Beijing. The same four names you'll find on a dozen tour packages.
        But the way Paris has a thousand tours and only a few that make you fall in love with it —
        the cities aren't what make a trip unforgettable. It's how you move through them, who you're with,
        and the moments no brochure ever mentions.
      </p>
      <span class="route-intro__pivot">This is that version of China.</span>
    </div>

    <button class="route-intro__toggle" id="route-intro-toggle" aria-expanded="false">
      Read the full story ↓
    </button>

    <div class="route-intro__expandable" id="route-intro-body">
      <div class="route-intro__body">

        <span class="route-intro__city-label">Shanghai</span>
        <p>Shanghai moves like a city that has not decided whether it is finished yet. The Bund is all colonial stone and river fog in the morning; the towers across the water are all glass and light after dark. In between is the French Concession — plane trees lining the sidewalks, a lane house converted to a coffee shop, the smell of rain on stone. You will eat something here that you cannot explain precisely and will think about for weeks afterward.</p>

        <span class="route-intro__city-label">Suzhou</span>
        <p>The classical gardens of Suzhou were designed to produce a specific feeling: the sense that you have stepped sideways into a different relationship with time. Water moves through them slowly. The framing of rocks and plants through windows is deliberate — each view is composed. Outside the gardens, the canal streets are narrow and wet-stone and still residential. Old women hang laundry over the water. The canal is working infrastructure, not scenery.</p>

        <span class="route-intro__city-label">Hangzhou</span>
        <p>West Lake is better at dawn than at any other time — before the tour boats, when the mist is still on the water and the Su Causeway is quiet enough that you can hear the birds. The tea fields above the city are a different kind of quiet: geometric and green and smelling of fresh leaf. In the afternoon the light on the lake is gold. The Song dynasty poets wrote about this and were not exaggerating.</p>

        <span class="route-intro__city-label">Beijing</span>
        <p>Beijing is a city that knows it is the capital. The scale is intentional — the long avenues, the monumental squares, the Forbidden City which is large enough that you cannot see across it. But what stays with you is the hutongs: the lanes behind the drum tower where old Beijing still exists at the pace it always had, where the smell of coal smoke mixes with cooking and someone's radio is playing Peking opera from a window two floors up.</p>

      </div>

      <p class="route-intro__colophon">
        10 days &nbsp;·&nbsp; 4 cities &nbsp;·&nbsp; Zero planning<br>
        All you need is curiosity and a half-empty suitcase.
      </p>
    </div>
  </div>
</section>

<script>
  const toggle = document.getElementById('route-intro-toggle');
  const body = document.getElementById('route-intro-body');
  if (toggle && body) {
    toggle.addEventListener('click', () => {
      const isOpen = body.classList.toggle('is-open');
      toggle.setAttribute('aria-expanded', String(isOpen));
      toggle.textContent = isOpen ? 'Close ↑' : 'Read the full story ↓';
    });
  }
</script>

  <section id="who-is-this-for" class="section" style="padding: 4rem 2rem;">
    <div style="max-width:48rem; margin:0 auto;">
      <h2 style="font-size:clamp(20px,3vw,30px); font-weight:700; font-family:var(--font-display); line-height:1.2; margin-bottom:1.25rem;">You don't need to know anything about China to come.</h2>
      <p class="muted" style="font-size:17px; line-height:1.85; margin-bottom:1rem;">No Mandarin. No experience. And all the stuff that sounds complicated — VPNs, payments, getting around — we've already figured out so you don't have to.</p>
      <p class="muted" style="font-size:17px; line-height:1.85; margin-bottom:1rem;">We were all first-timers once. All we ask is that you come with an open mind and a bit of curiosity.</p>
      <p class="muted" style="font-size:17px; line-height:1.85; padding-bottom:1.5rem;">That's genuinely enough.</p>
    </div>
  </section>

  <section id="itinerary" class="section card tone-sand">
    <h2 class="section-title">10-day route at a glance</h2>
    <p class="section-sub" style="margin-bottom:2rem;">Click any day to explore the places you'll visit.</p>

    <div class="itinerary-accordion">
      {itineraryWithSlides.map((d, idx) => (
          <div
            class={`itinerary-day${d.isFirstCityDay ? ' itinerary-day--city-start' : ''}`}
            data-day-idx={idx}
          >
            <button class="itinerary-day__header" aria-expanded="false" data-day-toggle>
              <span class="itinerary-day__num">{d.day}</span>
              <span class="itinerary-day__meta">
                <span class="itinerary-day__city">{d.city}</span>
                <span class="itinerary-day__title">{d.title}</span>
              </span>
              <svg class="itinerary-day__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="6 9 12 15 18 9"></polyline>
              </svg>
            </button>

            <div class="itinerary-day__body" data-day-body>
              <div class="itinerary-day__inner">
                {d.slides.length > 0 && (
                  <div class="landmark-carousel" data-carousel>
                    <div class="landmark-carousel__track-wrap">
                      <div class="landmark-carousel__track" data-carousel-track>

                        {d.slides.map((slide) => (
                          <div class="landmark-carousel__slide">
                            {slide.type === 'city' ? (
                              <div class="city-intro-card">
                                <img
                                  class="city-intro-card__img"
                                  src={slide.guide.heroImage}
                                  alt={slide.guide.heroAlt}
                                  loading="lazy"
                                />
                                <div class="city-intro-card__text">
                                  <span class="city-intro-card__label">City overview</span>
                                  <h3 class="city-intro-card__name">{slide.guide.city}</h3>
                                  <p class="city-intro-card__body">{slide.guide.context}</p>
                                </div>
                              </div>
                            ) : (
                              <div class="landmark-card">
                                <img
                                  class="landmark-card__img"
                                  src={slide.lm.image}
                                  alt={slide.lm.name}
                                  loading="lazy"
                                />
                                <div class="landmark-card__body">
                                  <h4 class="landmark-card__name">{slide.lm.name}</h4>
                                  <span class="landmark-card__name-zh">{slide.lm.nameZh}</span>
                                  <p class="landmark-card__text">{slide.lm.background}</p>
                                </div>
                              </div>
                            )}
                          </div>
                        ))}

                      </div>
                    </div>

                    {d.slides.length > 1 && (
                      <div class="landmark-carousel__nav">
                        <button class="landmark-carousel__btn" data-carousel-prev aria-label="Previous">←</button>
                        <div class="landmark-carousel__dots">
                          {d.slides.map((_slide, i) => (
                            <span class={`landmark-carousel__dot${i === 0 ? ' is-active' : ''}`} data-carousel-dot></span>
                          ))}
                        </div>
                        <button class="landmark-carousel__btn" data-carousel-next aria-label="Next">→</button>
                      </div>
                    )}
                  </div>
                )}
              </div>
            </div>
          </div>
      ))}
    </div>
  </section>

  <section class="section incl-grid">
    <article class="incl-card incl-card--yes">
      <h2 class="incl-card__title">Included</h2>
      <ul class="incl-list">
        {included.map((item) => (
          <li class="incl-item"><i class="incl-icon incl-icon--yes">✓</i><span>{item}</span></li>
        ))}
      </ul>
    </article>
    <article class="incl-card incl-card--no">
      <h2 class="incl-card__title">Not included</h2>
      <ul class="incl-list">
        {notIncluded.map((item) => (
          <li class="incl-item"><i class="incl-icon incl-icon--no">×</i><span>{item}</span></li>
        ))}
      </ul>
    </article>
  </section>

  <section class="section card tone-blue">
    <h2 class="section-title">Video references before you decide</h2>
    <div class="grid cols-3 video-grid">
      {recommendedVideos.map((video) => {
        const ytId = video.url.match(/[?&]v=([^&]+)/)?.[1] || '';
        return (
          <a href={video.url} target="_blank" rel="noopener" class="video-card">
            <div class="video-card__thumb">
              <img src={`https://img.youtube.com/vi/${ytId}/hqdefault.jpg`} alt={video.title} loading="lazy" />
              <div class="video-card__play">
                <svg width="48" height="48" viewBox="0 0 48 48" fill="none"><circle cx="24" cy="24" r="24" fill="rgba(0,0,0,0.6)"/><path d="M19 15l14 9-14 9V15z" fill="#fff"/></svg>
              </div>
            </div>
            <div class="video-card__info">
              <h3 class="video-card__title">{video.title}</h3>
              <p class="video-card__creator">{video.creator}</p>
              <p class="video-card__note">{video.note}</p>
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- SEO context — visually subtle, content-rich for search engines -->
  <section class="section" style="padding: 1.5rem 2rem 0;">
    <div style="max-width:54rem; margin:0 auto; border-top:1px solid #e8e5e0; padding-top:1.5rem;">
      <p style="font-size:13px; line-height:1.9; color:#9a9590; text-align:center;">Planning a China trip from Toronto or elsewhere in Canada? Start with the <a href="/handbook" style="color:#b8524a;">handbook</a>, then check the <a href="/about" style="color:#b8524a;">team background</a> and <a href="/privacy" style="color:#b8524a;">privacy policy</a>.</p>
    </div>
  </section>

  <section id="faq" class="faq">
    <div class="container">
      <div class="section-header">
        <p class="section-eyebrow">FAQ</p>
        <h2 class="section-title">Frequently Asked Questions</h2>
      </div>
      <div class="faq__list">
        {faq.map((f, i) => (
          <div class="faq__item" id={`trip-faq-${i}`}>
            <button class="faq__q" aria-expanded="false" aria-controls={`trip-faq-answer-${i}`}>
              <span>{f.q}</span>
              <svg class="faq__icon" width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M5 7.5L10 12.5L15 7.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
            </button>
            <div class="faq__a" id={`trip-faq-answer-${i}`} role="region" aria-labelledby={`trip-faq-${i}`}>
              <div class="faq__a-inner">{f.a}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section id="apply" class="route-apply">
    <div class="container" style="text-align:center; padding-top:0; padding-bottom:0;">
      <p class="section-eyebrow" style="color:rgba(255,255,255,0.6);">Apply</p>
      <h2 class="section-title" style="color:#fff; margin-bottom:1rem;">Ready to go?</h2>
      <p style="color:rgba(255,255,255,0.72); font-size:clamp(0.95rem,2vw,1.05rem); line-height:1.75; max-width:460px; margin:0 auto 2rem;">
        Tell us about yourself and which trip you're interested in — we'll follow up within 5 business days.
      </p>
      <a href="/apply"
        style="display:inline-block; background:var(--color-red,#c8102e); color:#fff; padding:0.9rem 2.5rem; font-size:0.875rem; font-weight:600; letter-spacing:0.08em; text-decoration:none; text-transform:uppercase; transition:background 0.2s;"
        onmouseover="this.style.background='#a00d24'"
        onmouseout="this.style.background='var(--color-red,#c8102e)'">
        Apply Now →
      </a>
    </div>
  </section>
</Layout>

<style>
.trip-hero {
  position: relative;
  height: 420px;
  overflow: hidden;
}
.trip-hero__img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
.trip-hero__overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, rgba(0,0,0,0.15) 0%, rgba(0,0,0,0.55) 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  padding: 2.5rem;
  text-align: center;
}
.trip-hero__title {
  color: #fff;
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 700;
  font-family: var(--font-display);
  line-height: 1.1;
  margin-bottom: 0.75rem;
}
.trip-hero__sub {
  color: rgba(255,255,255,0.85);
  font-size: 1.1rem;
  max-width: 640px;
  line-height: 1.7;
}
</style>

<script is:inline>
// ── FAQ accordion ──────────────────────────────────────
document.querySelectorAll('.faq__item').forEach(item => {
  const btn = item.querySelector('.faq__q');
  const answer = item.querySelector('.faq__a');
  btn?.addEventListener('click', () => {
    const open = item.classList.toggle('is-open');
    btn.setAttribute('aria-expanded', String(open));
    if (answer) answer.style.maxHeight = open ? answer.scrollHeight + 'px' : '0';
  });
});

// ── Itinerary day accordion ────────────────────────────
document.querySelectorAll('.itinerary-day').forEach(day => {
  const header = day.querySelector('[data-day-toggle]');
  const body = day.querySelector('[data-day-body]');
  if (!header || !body) return;
  header.addEventListener('click', () => {
    const open = day.classList.toggle('is-open');
    header.setAttribute('aria-expanded', String(open));
    body.style.maxHeight = open ? '900px' : '0';
  });
});

// ── Landmark carousels ─────────────────────────────────
document.querySelectorAll('[data-carousel]').forEach(carousel => {
  const track = carousel.querySelector('[data-carousel-track]');
  const slides = carousel.querySelectorAll('.landmark-carousel__slide');
  const prevBtn = carousel.querySelector('[data-carousel-prev]');
  const nextBtn = carousel.querySelector('[data-carousel-next]');
  const dots = carousel.querySelectorAll('[data-carousel-dot]');
  if (!track || slides.length === 0) return;

  let idx = 0;
  const total = slides.length;
  const slideWidth = 88;

  function goTo(n) {
    idx = Math.max(0, Math.min(n, total - 1));
    track.style.transform = `translateX(-${idx * slideWidth}%)`;
    dots.forEach((d, i) => d.classList.toggle('is-active', i === idx));
    if (prevBtn) prevBtn.disabled = idx === 0;
    if (nextBtn) nextBtn.disabled = idx === total - 1;
  }

  prevBtn?.addEventListener('click', () => goTo(idx - 1));
  nextBtn?.addEventListener('click', () => goTo(idx + 1));

  let touchX = 0;
  carousel.addEventListener('touchstart', e => { touchX = e.touches[0].clientX; }, { passive: true });
  carousel.addEventListener('touchend', e => {
    const delta = touchX - e.changedTouches[0].clientX;
    if (Math.abs(delta) > 40) goTo(idx + (delta > 0 ? 1 : -1));
  });

  goTo(0);
});
</script>
